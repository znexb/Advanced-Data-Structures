import type { StructureInfo } from '../types';

export const rbtInfo: StructureInfo = {
  title: 'Red-Black Tree',
  subtitle: 'A self-balancing BST with color invariants',
  badge: 'O(log n) guaranteed',
  notes: [
    'Every node is RED or BLACK. The root and NIL leaves are always BLACK.',
    'No red node may have a red parent (no two consecutive reds).',
    'Every path from any node to a descendant NIL leaf has the same number of BLACK nodes (black-height).',
    'These invariants guarantee height ≤ 2 log₂(n+1), so all operations are O(log n) worst-case.',
    'Insertion may require at most 2 rotations. Deletion may require O(log n) recolorings but at most 3 rotations.',
  ],
  useCases: [
    'Linux kernel: process scheduling (`CFS`) and virtual memory areas use red-black trees',
    'C++ `std::map` / `std::set` and Java `TreeMap` / `TreeSet`',
    'Database index structures needing strict worst-case bounds',
    'Computational geometry: interval trees, segment trees',
  ],
  funFacts: [
    'Invented by Rudolf Bayer in 1972 as "symmetric binary B-trees", renamed by Guibas & Sedgewick in 1978.',
    'A red-black tree is equivalent to a 2-3-4 tree: each black node + its red children form one 4-node.',
    'Deletion requires more cases (up to 6) than insertion (3 cases), making it notoriously tricky to implement.',
    'The Linux kernel\'s red-black tree implementation (lib/rbtree.c) is one of the most performance-tuned in the world.',
  ],
  complexity: [
    { operation: 'Search', average: 'O(log n)', worst: 'O(log n)' },
    { operation: 'Insert', average: 'O(log n)', worst: 'O(log n)' },
    { operation: 'Delete', average: 'O(log n)', worst: 'O(log n)' },
    { operation: 'Rotations on insert', average: 'O(1)', worst: 'O(1)' },
    { operation: 'Space', average: 'O(n)', worst: 'O(n)' },
  ],
  codeLines: [
    'type Color = "RED" | "BLACK";',
    '',
    'class RBTNode {',
    '  color: Color = "RED";',
    '  left:   RBTNode = NIL;',
    '  right:  RBTNode = NIL;',
    '  parent: RBTNode = NIL;',
    '  constructor(public key: number) {}',
    '}',
    '',
    'function leftRotate(T: RBT, x: RBTNode) {',
    '  const y = x.right;',
    '  x.right = y.left;',
    '  if (y.left !== NIL) y.left.parent = x;',
    '  y.parent = x.parent;',
    '  if (x.parent === NIL) T.root = y;',
    '  else if (x === x.parent.left) x.parent.left = y;',
    '  else x.parent.right = y;',
    '  y.left = x;  x.parent = y;',
    '}',
  ],
  githubUrl: 'https://github.com/Q-UW9/Advanced-Data-Structures/tree/master/assignments/02',
  repoDirUrl: 'https://api.github.com/repos/Q-UW9/Advanced-Data-Structures/contents/assignments/02',
};
