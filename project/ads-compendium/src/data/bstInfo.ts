import type { StructureInfo } from '../types';

export const bstInfo: StructureInfo = {
  title: 'Binary Search Tree',
  subtitle: 'A simple yet powerful ordered data structure',
  badge: 'O(log n) avg',
  notes: [
    'Every node satisfies the BST property: left subtree keys < node key < right subtree keys.',
    'In-order traversal of a BST always yields sorted output in O(n).',
    'Performance degrades to O(n) on sorted input — a degenerate linear chain.',
    'Foundation for self-balancing trees: AVL, Red-Black, Treap, Splay.',
  ],
  useCases: [
    'Symbol tables in compilers and interpreters',
    'Implementing `std::map` / `TreeMap` sorted key–value stores',
    'Event-driven simulation schedulers (ordered by timestamp)',
    'Auto-complete / prefix search in editors',
  ],
  funFacts: [
    'The number of distinct BST shapes with n keys equals the nth Catalan number: C(2n,n) / (n+1).',
    'A randomly built BST has expected height ≈ 4.31 log₂ n.',
    'The concept was first described by P.F. Windley, A.D. Booth, and others around 1960.',
    'An unbalanced BST on sorted input is functionally identical to a linked list.',
  ],
  complexity: [
    { operation: 'Search', average: 'O(log n)', worst: 'O(n)' },
    { operation: 'Insert', average: 'O(log n)', worst: 'O(n)' },
    { operation: 'Delete', average: 'O(log n)', worst: 'O(n)' },
    { operation: 'Min / Max', average: 'O(log n)', worst: 'O(n)' },
    { operation: 'Space', average: 'O(n)', worst: 'O(n)' },
  ],
  codeLines: [
    'class BSTNode {',
    '  constructor(public key: number) {}',
    '  left:  BSTNode | null = null;',
    '  right: BSTNode | null = null;',
    '}',
    '',
    'function insert(root: BSTNode | null, key: number): BSTNode {',
    '  if (!root) return new BSTNode(key);',
    '  if (key < root.key)',
    '    root.left  = insert(root.left,  key);',
    '  else if (key > root.key)',
    '    root.right = insert(root.right, key);',
    '  return root;',
    '}',
    '',
    'function search(root: BSTNode | null, key: number): boolean {',
    '  if (!root) return false;',
    '  if (key === root.key) return true;',
    '  return key < root.key',
    '    ? search(root.left,  key)',
    '    : search(root.right, key);',
    '}',
  ],
  githubUrl: 'https://github.com/Q-UW9/Advanced-Data-Structures/tree/master/assignments/01',
  repoDirUrl: 'https://api.github.com/repos/Q-UW9/Advanced-Data-Structures/contents/assignments/01',
};
